# Spectral ratio investigation — 2026-09-22

## Compact note — completed September 23

Orientation revision: the opening now states the small-gamma impracticality goal, explains repeated residual multiplication, and explicitly connects an upper ratio bound to a necessary step count. The numerical observation that lambda1 barely moves is replaced by its proved constant-direction Rayleigh lower bound, norm(B^T 1/sqrt(m))^2 >= 1, so constant lambda1 is not assumed. Main theorem/figure still fit on page 1; all calculations and the full appendix are preserved.

Final compression after Sam's content edits: main theorem/figure again fit on one page; approximately 273 prose words excluding equations and caption. The eigenvalue equality is folded into the Fourier integral. Finite endpoint formulas and normalization details are in Appendix E, including explicit definitions (E0). The main still discloses the corrected-matrix eigensolve and actual target-weight dependence. Caption now compares gamma4 necessary 9.20e11 with gamma64 sufficient 21,611; this establishes improvement using both interval sides. Figure height reduced to 3.8 inches using saved data. PDF has 14 pages including the expanded appendix. No training or new spectra.

Follow-up introduction revision: explicitly states the learning-rate choice, GD coefficient update and residual recurrence, exact error as a function of ratios and target weights, and the objective of enclosing convergence. Main equation numbers updated consistently; full Appendix A–G unchanged. Current PDF is 14 pages, with the figure on page 2 while content revisions are underway. No new calculations.

Produced `docs/frozen_geometry_capacity_access/gamma_ratio_note/gamma_ratio_note_compact.pdf`: a one-page main argument with one theorem and one two-panel figure, followed by the full reviewed derivation in Appendices A–G (13 pages total). The positive Fourier integral, two-sided finite ratio interval, and full error sandwich appear together in the main passage. Auxiliary eigenvalues are written as `lambda_i(S_gamma)` rather than beta. The mathematical reviewer checked the compressed theorem and the appendix assembly; no material issue remained. The figure worker replotted saved rank-26/33 intervals and necessary/actual/sufficient step counts in `compact_note/`; no training or eigensolves. Numerical checks are not promoted to certified enclosures, and no universal normalized-ratio monotonicity is claimed. Expanded Revision 4 is preserved unchanged.

## Note revision 4 — completed

Sam requested an updated PDF using the single-gamma, two-sided eigenvalue-ratio interval and its direct transfer to error and necessary/sufficient training-time bounds. Root owns the note and PDF; `revision4_interval_figures` owns a new isolated plot/data folder and reproduction script; `reviewer_gamma_mechanism` independently audits the math and final draft. Preserve prior PDFs. No optimizer training. The revision must replace the old anchored lower-ratio theorem, retain the four-part progression, distinguish actual target weights from ratio bounds, account for nullspace/unresolved mass, and avoid claiming universal monotonicity of actual finite ratios. Exact proofs and FP64 evaluations remain clearly separate.

Completed 12-page `gamma_ratio_note_v4.pdf` and versioned Markdown source in docs/frozen_geometry_capacity_access/gamma_ratio_note/. Canonical unversioned note/PDF updated to the same content; earlier versioned PDFs preserved. Theorem 1 gives the absolute two-sided interval; Section 4 applies its endpoints to the full residual and crossing-time sandwich. New full-target figure/counts use actual target weights, conservative handling of all unresolved energy, and a checked numerical allowance. At gamma4 the evaluated necessary/actual/sufficient counts are 9.206636e11, 1.007616e12, 3.303589e12. Independent quadrature and SVD checks pass. Mathematical and independent argument reviews found no remaining consequential defect; their reports are linked in the note folder README. Exact inequalities are not conflated with interval-certified numerics. All 266 mathematical objects rendered; all 12 pages and figure labels inspected; no unresolved placeholders, missing local links, or text beyond page margins. No optimizer training or commits. The PDF renderer now correctly handles adjacent display equations.

## General ratio-law investigation, September 23 — completed

Sam explicitly requested a moonshot agent to test a general normalized-eigenvalue ratio law from the Fourier multiplier, disprove it first if false, and prove the most useful justified relaxation for the actual small-gamma budget objective. Agent `moonshot_gamma_ratio_law` owns `docs/frozen_geometry_capacity_access/gamma_ratio_note/moonshot_ratio_law.md`. The brief includes the full debate, the independent review, the correct upper-bound direction, the existing failure of scalar Fourier-cutoff tightness, and the prohibition on polynomial-bound detours or long training. Coordinator handles the explanation of bound notation and assesses which current claims are ready for the note. No polished-paper changes are being made during this investigation.

Completed result: an exact symmetric, uniform two-sample counterexample disproves universal monotonicity of actual normalized ratios. A new scalar upper envelope for the actual finite tanh-plus-bias ratios is proved via generalized Cauchy singular-value decay and rank-one interlacing: lambda_i/lambda_1 <= min(1, 16 exp[-2 pi^2 (i-3)/(4 gamma X + log 16)]), i >= 4, where X is the sample-interval radius. This is rational approximation, not a polynomial-tail argument, and is not a derivation from multiplier positivity. Coordinator and `reviewer_gamma_mechanism` independently audited its normalization, constants, and hypotheses. It gives a useful low-gamma obstruction but is substantially looser than the existing integral bound. A tight scalar law derived directly from M_gamma remains unresolved. A shared feature-mean lower bound for lambda_1 supplies a monotone gamma-cap envelope for the existing integral comparison without assuming that actual ratios are monotone. Full proofs and limitations are in the moonshot report. Comparison figure and data: `moonshot_scalar_ratio/`; source: matching `moonshot_scalar_ratio.py`. Only saved spectra and target masses were reused; no training or new eigensolves. Numerical evaluations remain FP64 checks, not interval-certified results. The polished note and PDF are unchanged.

## Independent mechanism review, September 23 — completed

Coordinator: main task. Sam requested a fresh reviewer to assess whether the positive Fourier integral and the reference-free finite spectral bounds supply a defensible explanation of the small-gamma GD budget obstruction. Reviewer `reviewer_gamma_mechanism` independently checked the direct upper and corrected two-sided constructions, implementation, saved results, and older polished note. Full report: `docs/frozen_geometry_capacity_access/gamma_ratio_note/independent_mechanism_review.md`. Verdict: the direct upper comparison plus positive target-tail mass already supplies a useful necessary-time theorem; dismissing it because the integral spectrum is evaluated numerically was too strong. Universal monotonicity of actual finite normalized ratios or target crossing times remains unproved and is unnecessary for this small-gamma obstruction. Recommend the simpler direct upper bound in the main argument and corrected intervals as supporting finite-point comparisons. No validated tight closed scalar gamma/rank formula exists; the previously tested scalar Fourier-cutoff route remains too loose. Exact mathematical inequalities and checked FP64 evaluations must be distinguished from certified numerical intervals. No training, new sweep, or changes to the polished paper were made. The coordinator read the full review and checked its scope against the saved numerator, normalization, and lattice-error values.

Coordinator: main task. Scope: explain changes in normalized eigenvalues of the existing frozen tanh kernel. No optimizer training or changes to the laptop app.

Completed: exact finite-matrix derivative accounting, the Fourier derivative identity, explicit halo / mean-coupling / discrete-center terms, finite-change integration, and checks against the earlier sharp-step approximation. Independent read-only mathematical review confirmed the identities and supplied an exact counterexample to unrestricted ratio monotonicity.

Outputs: four PNG figures, `spectrum_mechanism_results.md`, and reproducible numerical data in `data/`. Source scripts are in the matching `experiments/.../gamma_solve/spectrum_mechanism/` directory. No training or changes to the laptop app. General rank-to-frequency bounds and continuous-interval certification remain open and are stated in the writeup.

## Objective clarified, September 23

Sam's objective is the small-gamma obstruction to stable, raw-coordinate readout GD reaching high precision even when a sufficiently accurate representation exists. An exact spectral predictor or universal ratio monotonicity theorem is not required. The mechanism must explain why the relevant normalized eigenvalues are tiny at small gamma and improve in the measured larger-gamma regimes, and connect those modes to required target error reduction.

Keep the sources distinct: the spatial constant/separation/boundary/grid split; this task's Fourier quadratic-form and derivative diagnostics (W=153, m=263); the original optimization PDF; and the new collaborator access PDF (primary W=559, m=8193). The latter adds positive slow-band target-energy bounds and necessary training times. It uses a corrected periodic feature construction, not this task's uniform-center integral.

Sam explicitly excludes the polynomial-bound route because its prior bounds were too loose. A newly drafted, unrun polynomial diagnostic script was removed. No polynomial results are part of the continuing argument.

A quick alternative Fourier-cutoff/min-max bound was tested without creating a new experiment or figure. It was too loose for the main explanation: at gamma 8, the rank-32 ratio upper estimate was about 5.9e-6 versus actual 4.93e-9 (roughly 1200 times larger). Do not promote it as a tight bound. The existing exact directional Fourier accounting and the collaborator note's positive slow-band result are the stronger current evidence.

Completed the positive-slow-band calculation on this task's own W=153, m=263 geometry (`slow_band_check.py`, `data/slow_band_check.json`). Gamma 4 has an explicit readout achieving relative error 5.85e-4, but a positive band with 2.735% target energy forces ~2.806e11 steps to 1% at eta=0.5/lambda_max. Full spectral forecast ~1.008e12, not executed. Independent SVD routines and 70-digit direct readout evaluation agree. Gamma 8 band bound ~3.038e6, forecast ~1.970e7; gamma 16/64 forecasts 66,209/18,272. This supplies the actual required-target/representability check alongside our own Fourier ratio diagnostics, without mixing the collaborator's geometry or using polynomial bounds. Details and limitations appended to spectrum_mechanism_results.md.

Added `step_prediction_check.py` and a fifth PNG, comparing the uniform-center integral's GD forecast with the actual finite-tanh spectrum on the same geometry. Same finite-model learning rate in both. The integral crossing counts differ by +0.13874%, +0.00280%, 0%, and -0.29006% for gamma 4, 8, 16, and 64. Saved counts and curves are in `data/step_prediction_check.{json,npz}`. These are eigendecomposition-plus-powers calculations; Sam explicitly ruled out lengthy PyTorch training, and no training was started. The integral calculation is separate from the collaborator's periodic Fourier predictor. Neither the trillion-step count nor the new comparison is interval-certified.

## Finite-change lower bound, September 23

Added `finite_ratio_bound.py`, `finite_ratio_bound.png`, `data/finite_ratio_bound.json`, and `finite_ratio_bound_results.md`. Fixed reference gamma 8; sweep 8–128; ranks 12, 20, 32. The all-vector finite-change Fourier identity avoids the zero-mean restriction of the earlier absolute-energy formula. A relative correction bound on the old leading eigenspace plus min–max gives a rigorous inequality for each new normalized eigenvalue without fixing its new eigenvector. At gamma 64 the FP64 lower bounds are 77%, 64%, and 41% of actual ratios. The correction and top-eigenvalue row-sum upper bound are measured from the new finite kernel; new eigenvalues are used only for comparison. These evaluations are not interval-certified and do not establish a general gamma-only law or universal monotonicity. No optimizer training was run.

Expanded per Sam's corrected layout: `finite_ratio_width_grid.py` produces two 4-by-4 PNGs and one JSON data file. Rows are reference gamma 16, 8, 4, 2; columns N=64,128,256,512; colors are i=N/32,N/16,N/8,N/4. Main figure overlays actual solid and lower-bound dashed curves; companion shows bound/actual. Small-gamma/deep-mode calculations below numerical resolution are gaps, not zeros. The bound can be far looser in the wider/smaller-reference cases; the original selected-rank tightness is not general. See the follow-up in `finite_ratio_bound_results.md`. All final outputs use the corrected fractional ranks, not the superseded 5,15,50,100 request.

Audit: independent feature construction and a second SVD algorithm agreed within 7.4e-10 relative error on the resolved values among 64 selected points. Actual curves are repeated exactly across reference rows because the final kernel has no reference-gamma dependence. Some deep starting subspaces are unresolved and yield basis-dependent bounds; the older grid includes computed trial spaces there and should not be read as accurately identifying those leading eigenspaces.

Added `finite_ratio_transitions.py`: one doubling PNG, two starting/final-gamma heatmap PNGs, and `data/finite_ratio_transitions.json`. Same width/rank choices, but explicit starting singular-value and cutoff-gap screens now exclude unresolved initial subspaces. Gray heatmap cells are unresolved; white cells violate the selected comparison domain gamma_b >= 2 gamma_a. Doubling curves mark every computed point. Numerical checks passed for all admitted bounds. No training or modifications to earlier figures.

Added presentation extracts: `eigenvalue_vs_alignment_steps.png` replots the original saved mixed-sine 1% step-count comparisons (fixed p versus fixed normalized eigenvalues, reference gamma 8, descending-rank matching). `kernel_gamma_comparison.png` shows actual finite kernels at gamma 4,8,16,64 on shared scales. Source `presentation_checks.py`; kernel arrays saved in data. The coefficient-control supports eigenvalue-ratio dominance for the large change in this threshold-crossing count, not irrelevance of target alignment at every tolerance. The final impracticality argument still requires target energy in slow positive modes and an attainable readout fit; existing `slow_band_check` provides both for mixed sine at gamma 4.

## Independent note comparison, September 23

Coordinator: main task. Completed: a fresh agent with no inherited conversation compared the updated `gamma_access_collaborator_note.pdf` (document A) with `gamma_ratio_note_v3.pdf` (document B), reading both PDFs and appendices in full. The brief identified neither author; earlier discussions, source code, and reviews were excluded. The reviewer prefers B for explaining changes in normalized finite-network rates, and A for error-controlled forecasting and validation against executed GD. The reviewer emphasizes that B still uses measured finite-matrix corrections and a separate integral-spectrum forecast, so its comparative theorem is not a gamma-only training-time law. Full report: `docs/frozen_geometry_capacity_access/gamma_ratio_note/fresh_comparison.md`. No notes, figures, or experiments were changed by this review.

## Combined residual theorem check, September 23

Sam chose our four-part explanation and asked to combine the ratio lower bound with the full residual formula before compressing the note to half a page, one theorem, and one figure. Added `combined_error_bound.py` and the organized `combined_error_bound/` results folder (two PNGs, one report, data and validation). Same mixed-sine target, W=153 and m=263. Every target component is retained; unresolved ratios receive zero decay. The full upper residual bound reaches 1% in all six comparisons. Sufficient/actual step-count factors for doubling 2→4, 4→8, 8→16, 16→32, 32→64 are 26.63, 5.99, 1.56, 1.36, 1.25. Fixed reference 8→64 gives 5.84. The dashed curves use the actual ratio theorem, not the older integral-spectrum forecast. Independent gesdd/gesvd checks agree in bound counts to 4.6e-8 relative or better; old selected-rank bounds and finite counts reproduce. These are FP64 evaluations, not interval certificates. No training or changes to the note/PDF. Main conclusion: useful at 1%, with substantial low-gamma slack and a nonzero bound floor from unresolved modes; not a machine-precision guarantee.
