# expF17 -- clean cross-method scaling suite (SPEC)

**Status: spec recorded from Sam 2026-08-31; decisions returned same day. Step 0 (halo) DONE. PINN arm DEFERRED at Sam's instruction. Not otherwise started.**

Supersedes the reading of Catherine's `cdeng/expF15`/`expF16` benchmark, which was found to be confounded (unequal dictionaries, unequal information, unequal tuning, oracle selection on the scored set, and two reference oracles above machine epsilon).

## 1. Purpose

Apples-to-apples scaling comparison of PINN-style representations at matched hidden-unit count, on a task suite with machine-precision oracles, under two regimes: fitting a known oracle, and solving the PDE without one.

## 2. Global rules

- **tanh everywhere.**
- **No polynomial block** in the baseline runs (Plots 1 and 2). Polynomials appear only in Plot 3.
- **Nothing selected on the test set.** Hyperparameters chosen on a held-out validation split; test error reported once. (This is the specific flaw in the prior benchmark.)
- **Equal tuning effort per method**, declared up front as a fixed grid size, not per-cell search.
- Every reported number carries error bars over 3 seeds.
- Shared axis scales across subplots; single legend at the top; task families share a colour cue in the subplot title.
- Legends outside the axes (house rule).

## 3. Methods

**Plot 1 axis (single hidden layer, N units, directly comparable):**
| method | inner weights | readout |
|---|---|---|
| QI-Radon | from theory | solved |
| QI-tensor | from theory (tensor product) | solved |
| ELM / random features | random | solved |
| 1-layer tanh PINN | trained (Adam -> L-BFGS -> NNCG) | trained |
| direct Fourier | integer frequencies, 2K+1 ~ N | solved |

Direct Fourier is always **computed and stored** but gated behind a plotting flag so it can be excluded from the figure without recomputation.

**Plot 2 only (not on the N axis):** BWLer, RBF, Chebyshev pseudospectral.

## 4. Domain and geometry matching (Sam's requirement)

- QI-tensor's square and QI-Radon's disk must cover **the same area**.
- The **halo/collar delta must match** between them.
- The **inner test region** must have the same area for both.
- Test metric: rel L2 on the inner region. QI-tensor scores on its own (square) version of that region; every other method uses the disk/inner region.

## 5. Scale axis

- Widths: **64, 128, 256, 512, 1024**, extensible later without recompute.
- Data:width ratio: **oversample 4x-5x collocation rows per neuron** (expF03 default; expH05 used 8x; expE01 used >2x). Pick one and hold it.
- Seeds: **3**. Generated by translating every data point by a small random epsilon vector (~1e-10), holding the data otherwise constant, plus 1-2 extra data points per additional seed. Extra seeds queueable.

## 6. The two regimes (the main axis)

- **Plot 1a -- oracle.** Fit the known closed-form solution directly. No PDE.
- **Plot 1b -- dynamic PDE solve.** No closed form assumed; solve as a PINN would. Use **expF03's winning nonlinear recipe** for both QI-Radon and QI-tensor: coarse-to-fine **cascade init** (converge at W/4, refit, polish), damped Gauss-Newton with backtracking on the stacked residual, flat lambda=0.25, rcond=1e-15. Same procedure for both, no extra tuning for either.

## 7. Tasks

The expF13 suite (convection c40/c80, reaction, wave, Burgers, Poisson-manufactured), plus **Darcy flow and other FNO tasks**.

**Oracle purity gate (Sam: "if there is more error than that, the whole project is null"):**
| oracle | measured accuracy | status |
|---|---|---|
| convection c40/c80, reaction, wave, poisson_man | residual ~1e-50 at 50 dps | PASS (verified) |
| Burgers via Cole-Hopf | residual ~1e-40 at 40 dps; 75 Gauss-Hermite nodes | PASS (built, verified, not yet landed) |
| Burgers via Chebfun .mat | 6.0e-6 at nodes / 1.9e-3 interpolated | FAIL -- replace with Cole-Hopf |
| poisson_cg (COMSOL) | 2.5e-5 (measured by MFS) | FAIL -- irreducible |
| dysts via mpmath | machine eps | PASS |
| dysts via DOP853 1e-13 | 1e-13 typical, Thomas 1.1e-11 | FAIL -- switch to mpmath |
| MacArthur | ~5e-14 (cross-family) | BORDERLINE, irreducible |
| Darcy / FNO | **unmeasured** | UNKNOWN -- blocks item 7 |

## 8. Engineering requirements

- **Iterative arrival.** Loop order: one task -> one method -> one width. Each landing is a complete, interpretable cell.
- **Resumable.** Crash-safe store keyed by (task, method, width, seed, regime); atomic append.
- **Replot on every landing** so progress is visible.
- **Queueable** extra widths, extra seeds, extra tasks without recomputing existing cells.

## 9. Plots

**Plot 1 (x2: oracle and dynamic).** Subplot per task. x = hidden units. y = rel L2 test error. One colour per method, error bars over seeds, shared scales, single top legend.

**Plot 2.** Best width from Plot 1, plus BWLer / RBF / Chebyshev. A table in the same format as the prior benchmark's, plus a bar chart per task in the Plot 1 layout, with error bars.

**Plot 3.** QI-Radon (red) and QI-tensor (blue) only, with and without the correct polynomial terms. Solid = no poly (reuse Plot 1 data), dotted = with poly. Horizontal line for the best Chebyshev result. Same layout.

## 10. Decisions (Sam, 2026-08-31)

- **poisson_cg: DROPPED.**
- **Darcy: two versions** -- a manufactured Darcy (machine-precision oracle) and the original FNO Darcy, the latter kept as the single acknowledged reference-capped exception.
- **QI-tensor uses the exact same solver as QI-Radon.** Both solve only for a readout vector, via the expF03 `common.py` Gauss-Newton + cascade warm start. No separate tensor machinery.
- **ELM and the PINN get random init**; that is their honest seed variance. Seed index drives jitter AND RNG together.
- **lambda is a QI artifact.** Other methods get literature-standard tuning protocols, declared per method type and applied identically across cells. (Research pending.)
- **Equal area confirmed** -- Radon on its disk, tensor on its square, matched areas. Sam's reasoning: testing Radon on a square penalises it at the corners; testing tensor on a disk penalises it at the edges. Claude concedes. Safeguard: keep each target's features inside the square's inscribed circle so neither domain gets a harder slice.
- **Ports/dependencies are acceptable** for BWLer/RBF/Chebyshev, but each implementation must be verified correct against a published or analytic result before it produces a number. Reuse expF03's reproduction-gate pattern.
- **"Best width" for the non-N methods** = simply their best configuration; if they do not vary, report straight.
- **PINN arm: HOLD.** Build everything else; Sam will say when to run it.

## 11. Step 0 RESULT -- the halo (done, 2026-08-31)

Sam's recollection was correct and understated. Sweeping halo x N x target on the lstsq readout (5 targets, N in {32..1024}, halo in {0..70}, lambda=0.25, oversample 4x, rel L2 on [-1,1] including the boundary):

- **halo = 8 is sufficient everywhere.** Errors at halo 8, 14, 30 and 70 agree to within a small factor in every cell.
- **The requirement is FLAT IN N** -- it does not grow.
- `default_halo(1024, 0.25) = 409`, so the current default is **51x over** at N=1024. Cells that plateau above the floor (runge N<=64, multiscale N=32, highfreq N=32) are flat across the entire halo range -- resolution limits, not halo limits.
- Prior evidence: expG01 saw halo>=5 hold the floor for sine at N=128; expF03 only tested halo=0 vs default ("load-bearing but binary") so it never probed the middle.

Figure: `halo_requirement.png`. Data: scratch `halo_sweep.json`.

**Consequence 1 -- the matched quantity is COUNT, not delta.** Because the requirement is flat in N, the invariant is the halo *count* (equivalently ~2 tanh transition widths, since 8 centers x lambda=0.25 = 2). In distance it shrinks as 16/N. Radon's collar of 1.6 at W=1024 works out to ~7 extra offsets per direction -- already the same magnitude, which is a good consistency check. So match Radon and tensor on halo COUNT.

**Consequence 2 -- BLOCKING: the tensor cannot be built below N ~ 1024.** With N hidden units total, the 2-D tensor gets sqrt(N) centers per axis, and halo 8 per side consumes 16 of them:

| N | centers/axis | halo | interior | viable |
|---|---|---|---|---|
| 64 | 8 | 16 | -8 | NO |
| 256 | 16 | 16 | 0 | NO |
| 1024 | 32 | 16 | 16 | marginal |
| 4096 | 64 | 16 | 48 | yes |
| 16384 | 128 | 16 | 112 | yes |

The width ladder 64-1024 therefore gives QI-tensor at most one usable point. **Recommend extending the ladder upward (4096, 16384) so the tensor has a curve.** This is a real result in its own right -- the halo cost compounds across axes -- not a bug.


## 12. Step 0c RESULT -- the halo is FREE (final policy, 2026-08-31)

Sam's decision: the halo is added ON TOP of the interior budget, never displacing interior
resolution. Interior = sqrt(W) nodes per line in both geometries. Test region back to r = 1
(the full region). Match on the CALCULATED total, i.e. each method is plotted at its true
total column count.

Measured (4 targets, interior W in {128,256,512,1024}, halo 0-12, lambda swept):

- **Radon saturates by halo 6**; 6 through 12 are flat to within floor noise.
- **The tensor keeps gaining a little to 12**, driven entirely by the oblique-ridge target
  (W=1024: 2.6e-11 at h=4 -> 1.2e-12 at h=8 -> 3.4e-13 at h=12), i.e. by the corner centers.
- **halo = 0 is catastrophic** for both (1e-7 to 1e-2). halo 2 buys 2-3 orders; 4-6 reaches the floor.
- **Sam's 8 / max 12 is correct under the free-halo framing.** The earlier `line/6`
  recommendation was an artifact of the fixed-budget sweep, where the halo displaced interior
  resolution and the curve turned back up. Under a free halo the curve is monotone-then-flat.

**Adopted: halo = 8 per side, both geometries.** Radon is indifferent above 6, and 8 keeps the
tensor within about one order of its best on the one target that still cares. A larger halo
would only shift both rightward on a column-based x-axis for no accuracy gain on Radon.

**Column accounting (structural, not bookkeeping).** Radon = n^2 + 2nh; tensor = n^2 + 4nh + 4h^2.
Radon's parameter space is (periodic angle) x (bounded offset), so only ONE coordinate has a
boundary and needs a halo. The tensor's is (bounded x) x (bounded y) -- two boundaries, hence two
strips plus four corners (the 4h^2 term). At interior W=1024, halo 8: Radon 1537 columns,
tensor 2401. This is why the two cannot simultaneously match on interior resolution, halo count,
AND columns. Resolution: plot each at its true total column count and let the points land where
they land.

**W = 128 is below resolution** (Radon sep_sine 2e-2) and W = 256 leaves Radon's oblique target at
4e-6 -- that is the expH05 direction cliff reproducing, since 16 spokes cannot align with a
36.9-degree ridge. Recommend the ladder start at 256 and read 256 as a cliff datapoint, not a
scaling point.

Figure: `halo_free.png`. Data: scratch `halo_free.json`.


---

# 13. FINAL PROTOCOL (signed off 2026-09-01) -- supersedes sections 2-12

## 13.1 Tasks
expF13 five: convection c40, convection c80, reaction, wave, Burgers (Cole-Hopf oracle).
Plus poisson_man, Darcy-manufactured, Darcy-original. **poisson_cg DROPPED** (COMSOL, 2.5e-5).
FNO scoping: Darcy x2 in; FNO-Burgers optional (2-D, needs data); FNO Navier-Stokes OUT (3-D).

## 13.2 Oracles
Verified exact at 50 dps against their own operators: convection c40/c80, reaction, wave,
poisson_man (residual ~1e-50). Burgers: **Cole-Hopf, 75 Gauss-Hermite nodes -> 2.7e-15**,
verified against the Burgers operator at 40 dps and an independent trapezoid rule; must be
landed in `problems.py` and the "No closed form" docstring corrected. Darcy-original is the
single acknowledged reference-capped exception (accuracy still unmeasured).

## 13.3 Coordinates -- no per-task translation needed
Every task is posed in scaled P = (xi, eta) in [-1,1]^2 with the physical map folded into the
operator coefficients: convection/reaction x = pi(xi+1), t = (eta+1)/2; wave x = (xi+1)/2;
Burgers x = xi; poisson (x,y) = P/2; Darcy [0,1]^2 -> [-1,1]^2. xi is always space, eta always
time. The geometry code never needs to know which task it is running.
**Exceptions:** poisson_man is the square MINUS four holes (mask with `in_poisson_domain`; the
tensor wastes ~13% of its centres in dead space -- report it separately, not averaged in).
Burgers eval grid moves to the 200x200 uniform grid the time problems use.

## 13.4 Regions
**Train on [-1,1]^2. Score on [-1,1]^2. Halo outside both.** Measured: training out to the halo
edge is 9x to 300,000x WORSE (density-matched), because the dictionary spends capacity on a
region nobody scores; it is also impossible in the dynamic arm (no PDE outside Omega), and
PINN_FEASIBILITY.md:11 lists "the halo needs no data outside Omega" as a PDE-specific bonus.
**ALSO report the inscribed-disk (r=1) error separately** -- see 13.10.

## 13.5 Geometry
- **Radon**: directions theta_j = pi*j/J (endpoint rule, axes included). Per-spoke extent
  +-T(theta) with T = |cos|+|sin| (the square's support function). **Uniform offset spacing h
  across all spokes** (offset count varies as 2T/h), single gamma = lam/h. Offsets symmetric
  about t=0, so every spoke's best-resolved band runs through the square's centre. Replaces the
  flat collar of 1.6, which wasted ~20% of offsets on average and 37% on the axis directions.
- **Tensor**: n_ax centres per axis over [-1,1], symmetric about the origin.
- **Exact column match**: J = n_ax, M = n_ax+2, +1 bias = (n_ax+1)^2 = tensor at n_ax/axis.
  Under uniform spacing this becomes sum_j(2T(theta_j)/h + 1 + 2*halo), matched by bisection on h.
- Halo cost is structural, not bookkeeping: Radon n^2+2nh (one periodic coordinate, one bounded);
  tensor n^2+4nh+4h^2 (two bounded coordinates, hence corners). Report column counts alongside.

## 13.6 Halo
Default `halo = min(n_ax/8, 8)` per side. **Per-method, validation-selected over {h0-2, h0, h0+2}**
-- identical effort, method-specific value, selected h recorded. Rationale: equal tuning EFFORT is
the fairness criterion, not equal hyperparameter VALUES (nobody forces tanh and gaussian to share
a lambda). Evidence for the default: at fixed columns the acceptable band intersection across both
geometries x four sizes is exactly {0.125}; at n_ax=64 it is exactly {8}, where the fraction branch
meets the independently measured 1-D count threshold. `default_halo`'s floor of 70 comes from the
mpmath convolution tail and does NOT apply to the lstsq path (51x over at N=1024).

## 13.7 Bandwidth, solver, data
- lambda from expC07's closed-form rule (kernel Fourier tail at the first grid harmonic = eps):
  **tanh 0.25, gaussian-RBF 0.53, ELM 0.25**. Fourier/Chebyshev/BWLer have no bandwidth.
- **rcond = 1e-15.** expF13 measured 1e-13 costing 35x on wave (`run.py:59`); expF03's cascade
  winner was also 1e-15. Truncated lstsq/SVD only -- **never form Phi^T Phi** (expA01: ~7 decades).
- **Data: exactly 4x the column count, shuffled** for numerical randomness.
- All hyperparameter selection on a held-out VALIDATION split, never the test set.

## 13.8 Scale
Widths (columns): **128, 256, 512, 1024, 2048.** 4096 and 8192 queued as extras -- note that at
2401 columns wave is at 7.5e-10 and convection 1.8e-9, and expF13 needed W=9216 for wave to reach
1.6e-13, so **this ladder measures descent slopes, not floors**. The bottom two rungs are
direction-cliff-dominated for Radon (~11 and ~15 spokes) and should not have a scaling law drawn
through them.

## 13.9 Seeds and statistics
3 seeds (more queueable). **Seed = grid-phase jitter + collocation RNG**: Radon direction phase
in [0, pi/J) and offset phase; tensor grid-origin shift per axis. These perturb the DICTIONARY,
never the problem, so they are legal for space-time and anisotropic operators. **No rotation**
(does not survive square domains or space-time). **No 1e-10 data jitter** (measures rounding noise
for a deterministic geometry and would fake-shrink ELM/PINN bars). ELM and the PINN get random init.
**Geometric mean everywhere; error bars = geometric standard deviation, [g/s_g, g*s_g].**

## 13.10 NEW -- the Radon angular field of view (Crowther), and dual scoring
With J views at offset spacing h, a Radon reconstruction is faithful only inside radius
**R_max = J*h/pi**. Measured across the whole ladder under the balanced J=M split:

| cols | n_ax | halo | n_int | h | J | R_max |
|---|---|---|---|---|---|---|
| 128 | 10 | 1 | 8 | 0.364 | 10 | 1.158 |
| 256 | 15 | 2 | 11 | 0.255 | 15 | 1.216 |
| 512 | 22 | 3 | 16 | 0.170 | 22 | 1.189 |
| 1024 | 31 | 4 | 23 | 0.116 | 31 | 1.142 |
| 2048 | 44 | 6 | 32 | 0.082 | 44 | 1.150 |

The square's inradius is 1.0 and its circumradius is sqrt(2) = 1.414, so **the corners (21.5% of
the area) are angularly undersampled at every width -- and R_max is scale-invariant** (J ~ n and
h ~ 1/n, so J*h does not grow with budget). Radon therefore carries a fixed corner penalty on a
square that NO amount of budget removes under a balanced split. R_max scales with the aspect ratio
J/M, not the total; reaching the corners needs **J/M ~ 1.23**.

Consequences, all three adopted:
1. **Score on the full square (headline) AND on the inscribed disk r=1 (secondary).** The square
   keeps the IC/BC in the metric and stays comparable to expF13; the disk isolates Radon's real
   capability. Reporting both makes the corner effect attributable instead of silently capping
   Radon's curve and being misread as a method limit.
2. Keep balanced J=M as the headline split (the tensor is structurally symmetric; letting Radon
   optimise a split the tensor cannot access breaks apples-to-apples).
3. Add **two extra Radon lines**: J/M = 1.23 (corner-corrected) and the expH05-optimal split
   (M* ~ B^0.27..0.44), so what balance costs Radon is visible in the same plot.

## 13.11 Two regimes
- **Oracle**: fit the known solution.
- **Dynamic**: expF03's recipe, identically for both QI methods, no extra tuning for either --
  coarse-to-fine **cascade init** (converge at W/4, refit, polish) + damped Gauss-Newton with
  backtracking on the **stacked** residual + flat lambda=0.25 + rcond=1e-15. Canonical
  implementation `expF03/common.py`, which ships a reproduction gate requiring it to reproduce
  recorded expF01/expF02 cells first -- reuse that gate for every ported method.

## 13.12 Plots
1a/1b: subplot per task, x = columns, y = rel L2 (geomean, GSD bars), one colour per method,
shared scales, single top legend, task families colour-cued in the title.
2: best configuration per method plus BWLer/RBF/Chebyshev, as a table and a bar chart in the
Plot 1 layout.
3: Radon (red) and tensor (blue), solid = no polynomial, dotted = with; horizontal line at best
Chebyshev. **RBF is exempt from the no-polynomial rule** -- polyharmonic splines and multiquadrics
are only conditionally positive definite, so the tail is required for solvability.
Polynomial audit: only checkpoint F uses the degree-3 block (MONO_1D=[0,1,2,3], MONO_2D = 10
monomials of total degree <=3); checkpoints E and H use a bias column only.

## 13.13 Engineering
Iterative arrival (one task -> one method -> one width per landing). Resumable, crash-safe store
keyed by (task, method, width, seed, regime), atomic append. Replot on every landing. Extra
widths/seeds/tasks queueable without recompute.

## 13.14 Still open
- BWLer: port the JAX repo, or use explicit-BWLer (mathematically the Chebyshev arm -- possible duplicate).
- Darcy-original oracle accuracy: unmeasured, needs a path to the .npz files.
- Literature tuning protocols for RBF / Chebyshev / ELM / Fourier / BWLer: PARKED at Sam's
  instruction until everything else is agreed.
- Tensor corner blocks (4h^2 columns): proposed for dropping as a cheaper variant, untested.
- PINN arm: HOLD until Sam says go.

## 13.15 Action items outside expF17
- Land the Cole-Hopf Burgers oracle in `expF13/problems.py`; correct the "No closed form" docstring.
- Point Catherine's dysts oracles at `reference()` (mpmath) instead of `rk_trajectory()` -- her
  DOP853 references carry 1e-13, and 1.1e-11 on Thomas. Cache is warm for 7 of 8. MacArthur has no
  mpmath route and is stuck at ~5e-14. **Note: dysts are NOT part of expF17's task list** -- this is
  an expF14/expF16 fix.
- Catherine's expF16 script is broken against HEAD: `SYSTEM_ORDER + EXTRA_DYSTS` now double-counts
  three systems through different code paths (SYSTEM_ORDER grew 5 -> 8 on 2026-08-31).
- expF13's writeup understates its own Poisson-CG result: the nodal ruler is 2.5e-5 (measured by
  MFS), not the ~1e-2 recorded, and expF13 scores at the nodes -- so 9.4e-5 is above its ruler,
  not below its noise. Correct `problems.py` and the writeup.
- expF01/expF03's Radon collar is under-provisioned (3.2-6.0 halo offsets vs the 8 needed); the
  per-spoke geometry in 13.5 should improve existing checkpoint-F 2-D results independent of expF17.
- The endpoint-rule direction change (11 sites, 10 files) is applied in the working tree but
  UNCOMMITTED.


---

# 14. THE RECIPE (2026-09-01) -- supersedes 13.10 and the method list in 13.1

## 14.1 Crowther is REJECTED; balanced J=M stands
I proposed sizing Radon so its resolved disk (R_max = J*h/pi) covers the square's circumradius,
giving J/M ~ 1.3. **Measured, and it is 9-15x WORSE at the budget that matters.** At C=2048:

| J | M | J/M | R_max | convection | wave | reaction |
|---|---|---|---|---|---|---|
| 38 | 54 | 0.70 | 0.75 | 1.28e-08 | 1.76e-10 | 1.60e-05 |
| 45 | 46 | 0.98 | 1.11 | **1.14e-09** | **3.54e-10** | **1.94e-06** |
| 52 | 39 | 1.33 | 1.51 | 1.00e-08 | 5.46e-09 | 1.48e-06 |
| 68 | 30 | 2.27 | 2.62 | 8.53e-06 | 7.92e-06 | 1.24e-05 |

J/M=1.33 satisfies Crowther (R_max >= sqrt2) and still loses to balanced, which "fails" it.
Crowther describes angular aliasing when INVERTING measured projection data; a least-squares fit
in a ridge dictionary has no hard field of view. Borrowed theory, does not transfer.
Consistent with expH05 (alpha = 0.27-0.44, directions growing slower than sqrt(B)).
**Balanced J = M is the default**, the exact-column identity is unchanged, and no second scoring
region is needed. Note the regime dependence: at C=512 the optimum IS J/M=1.3 -- more directions
help when resolution-starved, hurt once resolving. Hence J/M is a KNOB, not a constant.

## 14.2 One recipe for every (task, method, budget, seed)
1. Build the dictionary at column budget C from the method's default configuration.
2. Sample exactly 4C training points, shuffled; hold out a validation split.
3. Sweep each knob one step either side of its default (factorial, <=27 cells); select on
   VALIDATION. expF03: "knobs are not additive", so factorial beats one-at-a-time.
   **TUNE ONCE per (task, method, width). Freeze the configuration, THEN run the seeds.**
   Never tune per seed -- that would let each seed pick its own best and collapse the error bars
   into a measure of the tuning grid rather than of the method.
4. Solve by truncated lstsq, rcond = 1e-15. Never form Phi^T Phi.
5. Score on the full square (masked for poisson_man).
6. Record error, selected configuration, and actual column count.

**Every derived rule is a DEFAULT and a sweep centre, never a binding constraint.** The Crowther
episode is why: a rule derived from adjacent theory was wrong by an order of magnitude and only a
five-minute measurement caught it.

| method | knobs | default | sweep |
|---|---|---|---|
| QI-Radon | lambda, halo, J/M | 0.25, min(n_ax/8,8), 1.0 | +-20%, +-2, x{0.7,1.0,1.4} |
| QI-tensor | lambda, halo | 0.25, min(n_ax/8,8) | +-20%, +-2 |
| ELM | lambda | 0.25 | +-20% |
| tensor-product spectral | none | modes/degree from C | -- |
| RBF | lambda, poly degree | 0.53, 3 | +-20%, {1,2,3} |

Equal effort per knob a method actually has. Fourier is not disadvantaged by having none.

## 14.3 Method list collapsed 8 -> 5
**Direct Fourier, Chebyshev pseudospectral and explicit-BWLer are the same object**: a
tensor-product spectral basis differing only in which 1-D basis sits on which axis (Catherine's
`bwler_pde` implemented exactly this, Fourier-where-periodic x Chebyshev-otherwise). Running all
three runs one arm three times. Pure 2-D Fourier would also Gibbs on wave/Burgers (Dirichlet) and
reaction (C^1 seam), producing flat useless lines.

- **Scaling axis**: QI-Radon, QI-tensor, ELM, [1-layer tanh PINN -- ON HOLD]
- **Comparators (Plot 2)**: tensor-product spectral (Fourier where periodic, Chebyshev otherwise),
  RBF, [BWLer-hat only if the JAX repo is ported -- a genuinely different object]

## 14.4 Per-task annotations (not exceptions -- one recipe still runs all of them)
- **poisson_man**: perforated domain, masked for train and score. Grid-based methods (tensor,
  spectral) waste ~13% of their basis inside the holes and are structurally disadvantaged.
  Report separately, never averaged in.
- **reaction**: C^1 kink at the periodic seam, so every method converges algebraically, not
  geometrically. Expected.
- **Burgers**: shock-limited; nothing resolves it on this ladder. Known-hard case, kept.
- convection c40/c80, wave, Darcy-manufactured, Darcy-original: clean.


---

# 15. TUNING PROTOCOLS (literature research, 2026-09-01) -- supersedes the knob table in 14.2

Shape is identical for all families: **one scalar knob -> closed-form default -> 3-point
log-spaced check on the held-out validation split -> argmin -> FREEZE, then run seeds.**
Each check point is one lstsq solve. Families 3 and 4 have no scalar knob; that is the finding,
not a gap.

## 15.1 RBF
- **Kernel: IMQ** phi(r) = (1+(eps r)^2)^{-1/2}. Positive definite, so **NO polynomial tail is
  required** and less flat-limit-fragile than the Gaussian.
- **Default eps*h = 1.** Sits between the two published closed-form rules on [-1,1]^2 with an
  n x n centre grid: Hardy (1971) gives eps*h = 1.23, Franke (1982) gives 0.57. No search needed.
- **3-point check: eps*h in {0.5, 1, 2}.**
- Rippa's LOOCV (Rippa 1999) is the canonical selector -- closed form e_k = c_k/(A^{-1})_{kk},
  one factorization per candidate, no refits; PRESS r_i/(1-H_ii) is the rectangular-lstsq
  equivalent. We do not need it at a 3-point grid, but cite it as the standard.

**CORRECTION to 13.12: RBF is NOT exempt from the no-polynomial rule.** The exemption I wrote was
based on polyharmonic splines and multiquadrics being only conditionally positive definite. IMQ is
positive definite (CPD order 0) and needs no tail, so the RBF arm runs polynomial-free like every
other method and the baseline stays uniform. Tail requirement by kernel: Gaussian/IMQ/Wendland none;
multiquadric degree 0; PHS r/r^3/r^5 degree 0/1/2; TPS r^2 log r degree 1.

**KNOWN LIMITATION, must be stated in the writeup:** rcond-truncated lstsq **cannot reach RBF's own
ceiling.** The accuracy-optimal eps lies in the flat regime where RBF-Direct is catastrophically
ill-conditioned -- and that ill-conditioning is an artifact of the BASIS, not of the interpolant,
which stays well behaved as eps -> 0. RBF-QR (Fornberg/Larsson/Flyer 2011) and Contour-Pade exist
precisely to replace truncated SVD there. So our RBF number is a lower bound on what RBF can do.
Cite it; do not try to fix it inside this benchmark.

**Second RBF entry, zero tuning: PHS(r^3) + degree-1 polynomial.** Polyharmonic splines are
scale-invariant and have NO shape parameter at all; the polynomial degree sets the convergence
order (Flyer/Fornberg/Bayona/Barnett, JCP 2016). This is the modern literature default precisely
because it removes shape-parameter tuning. Guideline: point count >= 2x the number of polynomial
terms. Recommended as a second RBF arm -- it sidesteps both the tuning question and the flat-limit
conditioning problem.

## 15.2 ELM / random features
- **Parameterization: i.i.d. W_ij, b_i ~ U(-R, R), tanh, inputs affinely mapped to [-1,1]^2.**
  This is the standard ELM/RFM convention (Dong & Yang 2022; Chen, E & Luo 2022).
- **Default R = 2.** Between RFM's published R=1 and Dong & Yang's single-hidden-layer optima of
  1.82-3.36. **3-point check: R in {1, 2, 4}.** Dong & Yang show accuracy is flat near the optimum
  and explicitly recommend rounding to a nearby "nice" value.

**CORRECTION to 14.2:** I had ELM at lambda = 0.25 via expC07. That is the wrong parameterization --
expC07's lambda is gamma*h on a UNIFORM GRID, whereas ELM draws i.i.d. weights with no grid and no h.
Published R values do not transfer between the two. Use the standard convention above and say so.

**Re-select R per width.** For a single hidden layer, the optimal R *increases* with parameter count
(Dong & Yang Fig. 2b); their near-invariance claim applies to depth >= 2. Our per-(task, method,
width) selection already handles this -- do NOT collapse it to one global R. Fix the RNG seed across
the selection run and the production run, or the 3-point comparison is noise. The literature selects
on the training residual; we use validation instead, since the training residual collapses to zero
on an underdetermined fit.

## 15.3 Tensor-product spectral
**Confirmed: literally zero hyperparameters.** No shape parameter, no scale, no initialization, no
randomness. Chebyshev-Gauss-Lobatto nodes x_j = cos(j*pi/N) (endpoints included so BCs are
imposable); Fourier on periodic axes, Chebyshev otherwise (Boyd; Trefethen). Mode count / degree is
fixed by the column budget. The only free choice -- collocation points per unknown -- is set
globally by expF17, so it is a benchmark-level constant, not a method knob.
**State this explicitly in the writeup** so the equal-effort rule is visibly not violated by this
arm having nothing to tune. Rectangular/least-squares reference: Driscoll & Hale 2016.
Do NOT use the Kosloff-Tal-Ezer map; it only relieves explicit time-stepping restrictions.

## 15.4 BWLer
- **Default: explicit BWLer, spectral derivatives (`fd_k=None`), CGL nodes, nodal collocation.**
- **Under a direct lstsq solve, BWLer's entire optimizer hyperparameter list vanishes** (Adam lr,
  NNCG preconditioner rank 16-1000, CG steps 16-2000) because the e^{-t/kappa^2} optimization term
  in their Theorem 5.1 is zero. So this arm is genuinely equal-effort with the others.
- 3-point check `fd_k` in {spectral, 5, 3}, run once to document then freeze. Spectral should win by
  Theorem 5.1 -- with no optimization gap the FD misspecification term N^{-(k+1-d)} is pure loss.
  The FD option only ever buys conditioning for a *gradient* optimizer.

**PARTIAL REVERSAL of 14.3.** I collapsed Fourier + Chebyshev + explicit-BWLer into one arm on the
grounds that they span the same space. The span claim is correct -- barycentric Lagrange on N+1 CGL
nodes IS P_N, and BWLer's spectral matrix IS the Chebyshev differentiation matrix -- but the
**parameterization differs** (value space vs coefficient space) and that difference is measurable:
the BWLer paper's own Chebyshev-least-squares baseline (`chebfit`, Appendix C.1.3) is beaten by
explicit BWLer *as the least-squares problem becomes more ill-conditioned* (their Fig. 4). Under
rcond-truncated lstsq that is exactly the regime we are in. **Keep both, labelled honestly as two
parameterizations of one method, not two competing methods.**

**BWLer-hat is EXCLUDED.** It is an MLP producing nodal values, hence nonlinear in its parameters,
so it cannot run under a pure lstsq solve. Including it would require gradient training and break
both the solve frame and the equal-effort rule. Run it as a separate side experiment or not at all.

**FAIRNESS NOTE, one sentence required in the writeup:** BWLer's actual contribution is about
*trainability by gradient methods*; the paper concedes its results are "not time- or
parameter-matched" and that BWLer "inherits the weaknesses of classical spectral methods, including
handling PDEs with sharp solutions or problems over irregular domains." An lstsq benchmark therefore
measures the axis on which BWLer has the *least* distinguishing content. Our result must not be
presented as a refutation of that paper.

## 15.5 Revised knob table (supersedes 14.2)
| method | knob | default | 3-point check |
|---|---|---|---|
| QI-Radon | lambda, halo, J/M | 0.25, min(n_ax/8,8), 1.0 | +-20%, +-2, x{0.7,1.0,1.4} |
| QI-tensor | lambda, halo | 0.25, min(n_ax/8,8) | +-20%, +-2 |
| ELM | R (i.i.d. U(-R,R)) | 2 | {1, 2, 4} |
| RBF (IMQ) | eps*h | 1 | {0.5, 1, 2} |
| RBF (PHS r^3 + poly1) | none | -- | -- |
| tensor-product spectral | none | CGL, Fourier-where-periodic | -- |
| BWLer (explicit) | fd_k | spectral | {spectral, 5, 3}, then freeze |


---

# 16. REVIEW CORRECTIONS (independent review, 2026-09-01) -- binding

An independent reviewer with fresh context re-ran the measurements and audited the doc.
**Verified correct and not to be re-litigated:** every expF16 criticism (plus one more -- `als_qi`
forms `DA.T @ DA`, the normal equations expA01 measured at ~7 decades); the 14.1 Crowther table
(replicates digit-for-digit); the 13.4 train-extent result (8.8x to 312,000x, density-matched);
section 12's halo_free numbers; every repo citation; the Cole-Hopf existence claim and its
verification method; and the fairness architecture -- **no remaining setup artifact favours either
QI method.** The residual asymmetries are real method properties, correctly surfaced.

## 16.1 BLOCKER -- the dynamic arm has no selection criterion, and leaks the oracle
14.2 says "select on VALIDATION", but in regime 1b there is no oracle *by premise* -- while oracles
exist for every task, so an implementer can silently validate against the exact solution and quietly
destroy the regime. `PINN_FEASIBILITY.md` also warns that residual is not a safe ranker: "A small
residual certifies nothing... Judge a solve by nested-width self-consistency."
**Compounding it: every step-0 default (halo, J/M, lambda) was measured in the ORACLE arm only.**
Nothing checks transfer to the PDE-residual system, where the operator differentiates the dictionary
and boundary rows carry their own weights.
**REQUIRED:** define dynamic-arm selection explicitly -- held-out collocation residual AND/OR
nested-width self-consistency, never the exact solution -- and run one dynamic-arm spot check of
halo and J/M at a single width before freezing. The Crowther episode is the argument for the check.

## 16.2 Radon geometry: adopt the variant that was actually MEASURED
13.5 specifies **uniform offset spacing h across spokes** (count varying as 2T/h). Every supporting
script -- `geom_setup.py`, `cap_check.py`, `split.py`, `train_extent.py` -- implements **per-spoke
spacing h_j = 2T(theta_j)/(n_int-1) with EQUAL COUNTS per spoke and per-spoke gamma_j**.
Consequences of the mismatch: (i) the exact-column identity is exact only for the equal-count
variant -- 14.1's "the exact-column identity is unchanged" is **wrong** under uniform spacing, where
it degrades to approximate bisection on a step function; (ii) M, and therefore the J/M knob, is
undefined under uniform spacing; (iii) all halo and split evidence was collected under the other
geometry. The doc records no motivation for uniform spacing, and the stated rationale (collar waste)
is delivered by the **per-spoke extent alone**, which both variants share.
**DECIDED: use per-spoke extent +-T(theta_j) with EQUAL OFFSET COUNTS per spoke and per-spoke
gamma_j = lambda/h_j.** Identity stays exact, J/M stays well-defined, and every measurement applies.
Supersedes the uniform-spacing sentence in 13.5 and the bisection note.

## 16.3 Halo evidence: overstated, and collected under the wrong protocol
- **"the intersection is exactly {0.125}" and "at n_ax=64 exactly {8}" DO NOT REPRODUCE** over all
  tasks. At a 3x-of-best threshold the tensor/reaction cells give {0.025-0.075} at n_ax=40 and
  {0.042, 0.062} at 48; the n_ax=64 claim needs a hand-picked ~2.4x threshold. I computed those
  intersections over convection+wave only and then wrote them as if they held generally.
  **The rule is a LOOSE FIT costing up to 8.2x above per-cell best before the sweep and ~3.5x after
  it** -- state it that way. As a default plus per-method validation sweep it is still the right
  rule; the derivation language was wrong.
- **All halo evidence is single-seed, oracle-arm, with lambda selected on the TEST region, 3x data
  (protocol says 4x), and -- except split.py -- at rcond=1e-13 (protocol says 1e-15, and expF13
  measured 1e-13 costing 35x near floors).** REQUIRED: re-run `geom_setup.py` and `cap_check.py` at
  rcond=1e-15, 4x data, 2-3 seeds, lambda on validation, before freezing. Minutes of compute.
- Section 11's "halo=8 is sufficient everywhere" is contradicted by its own data (for `exp`, h=8 is
  12-20x above best at N>=512 and the script's own 2x criterion demands 20-30). Superseded section;
  the final 2-D rule stands, but the sentence would mislead anyone reconstructing the chain.

## 16.4 14.1's Crowther verdict flattens a mixed result
Correct reading: J/M=1.33 is **8.8x worse on convection, 15.4x worse on wave, and 1.3x BETTER on
reaction** (1.48e-6 vs 1.94e-6 -- the bolded cell in 14.1's table is not the row minimum; fix it).
The conclusion is unchanged and in fact strengthened: balanced is the default, J/M is a
**task-dependent** knob, not a constant.

## 16.5 Archive the evidence (house rule)
`split.py` -- the single most consequential reversal in this doc -- prints only, with no saved JSON
and no figure. Same for `verify_analytic.py` and `verify_colehopf.py`. REQUIRED: re-run, save JSON
plus one figure each into this results directory.

## 16.6 Specification gaps that would make two implementers build different things
- **Spectral arm shape at budget C is undefined** ("modes/degree from C"). Anisotropic tasks
  (convection c80) care about the n_x vs n_t split. Either declare the balanced-shape rule or give
  spectral an aspect knob analogous to Radon's J/M.
- Validation-split fraction, whether it comes out of the 4C, and whether we refit on the full set
  after selection: all unspecified.
- Halo sweep {h0-2, h0, h0+2} needs a clamp rule at h0 <= 2.
- `min(n_ax/8, 8)` should read `round(n_ax/8)`, which is what the scripts implement.
- **Ladder columns are NOMINAL**: 128/256/512/1024/2048 are actually (n_ax+1)^2 = 121/529/2025 etc.
  Plots must use actual column counts, as 13.5 already promises.
- **convection_c80 has no step-0 measurement**; its defaults are extrapolated from c40.
- **Darcy-manufactured's oracle is not defined anywhere yet.**

## 16.7 Reversals of my own earlier calls
- **Keep the inscribed-disk metric, recorded but non-headline.** 14.1 dropped it once Crowther was
  rejected, but its diagnostic value survives: if Radon's curve flattens at large C, the disk metric
  is what attributes that to corners rather than to the method. It is free at analysis time from the
  stored eval points.
- **Promote 4096 (and 8192 for wave) from "queued" to PLANNED for the clean tasks.** A
  machine-precision paper whose flagship figure shows no method reaching 1e-13 anywhere invites the
  obvious attack.
- **The cliff annotation understates**: convection falls 3.5 decades between n_ax=24 and 40, so the
  512 rung is also pre-cliff, not just the bottom two.
- 13.9's "would fake-shrink ELM/PINN bars" is worded backwards -- 1e-10 data jitter would fake-shrink
  the *deterministic* methods' bars. Decision right, reasoning mis-stated.

## 16.8 Cleared
- `constructions/tensor_qi_2d.py`'s `np.longdouble` no-op on Apple Silicon and its weak test
  (asserts fp64 < 1e-7) are CONFIRMED -- but that module is **not in the expF17 pipeline** (section
  10 routes the tensor arm through the same lstsq/GN solver as Radon). Repo hygiene, not benchmark
  validity.
- The 75-Gauss-Hermite-node figure was verified only on the t=0.5 slice; `geom_setup.py` used n=200.
  **Re-verify 75 nodes against GH-600 on the full 200x200 eval grid before landing in `problems.py`.**
- Halo paid-vs-free: section 12 records "free" as Sam's decision and 13.5/13.6 use "paid".
  **Sam confirmed PAID in conversation** ("we should not have halo free"). Section 12 is superseded
  on this point; the free-halo column formulas in 13.5 are retained only as the explanation of WHY
  the two geometries have different halo costs, not as an accounting.


---

# 17. SAM'S RULINGS on section 16 (2026-09-01) -- binding, supersede 14.2/15.5 where they conflict

## 17.1 Tuning may use the full set. No held-out validation split.
**Sam's explicit call:** "this is pinns, not regular ml. it is fine to tune on everything."
Rationale: we are not measuring generalization. The PDE is fully specified and the oracle is known;
the quantity of interest is the achievable precision floor, which IS the best over configurations.
There is no unseen data for a hyperparameter to leak from.
**Consequence: equal DECLARED tuning effort becomes the sole fairness mechanism, so it now carries
all the weight.** Catherine's benchmark selected on the scored set AND had unequal effort (40 configs
vs 6) -- the unequal effort was the defect, not the selection. Record the chosen configuration for
every cell so the effort is auditable. This also removes the validation-split fraction, the
refit-on-full question, and the dynamic-arm oracle-leak concern (16.1) entirely.

## 17.2 Dynamic arm: tune the Gauss-Newton until it works
**Sam's call:** "tune the GN so that it actually works... go in and just figure it out till it works
(but don't spend too much time on it)." Start from expF03's cascade recipe (converge at W/4, refit,
polish; backtracking damped GN on the stacked residual; rcond=1e-15) and adjust until it converges
on each task. Same procedure applied to Radon and tensor, no per-method extra effort. Time-box it.

## 17.3 lambda is BAKED IN at 0.25. No sweep.
Evidence: expC03 (lambda* ~ 0.25, "essentially constant in width", flat basin); expC02 (constant
across frequency and width, with the drift reading explicitly corrected as jitter); expC07 (closed
form); expF03 on the actual PDE tasks ("lambda=0.25 flat survives. Basin 0.20-0.30, cliff at >=0.35").
Known cost: expF03 measured flat-0.25 staying within 20x (usually 4x) of the per-problem optimum,
and attributes the extreme cases to Newton-basin luck under a bad init rather than true sensitivity.
**ELM is the exception and it is NOT a lambda**: its parameterization is W,b ~ U(-R,R) with no grid
and no h, and Dong & Yang measured the optimum INCREASING with width at a single hidden layer.
R stays swept over {1,2,4}. Treating it identically to lambda would be the unfair choice.

## 17.4 No re-measurement before building
16.3's re-run requirement is WAIVED by Sam. The halo evidence stands as collected (single-seed,
oracle-arm, 3x data, mostly rcond=1e-13). The doc's language is still corrected per 16.3 -- the rule
is a loose fit costing <=8.2x before the sweep and ~3.5x after, NOT an exact derivation.

## 17.5 Resulting knob table (final)
| method | knobs | default | sweep |
|---|---|---|---|
| QI-Radon | halo [+ J/M, pending 17.6] | round(n_ax/8) capped 8 | +-2 |
| QI-tensor | halo | round(n_ax/8) capped 8 | +-2 |
| ELM | R | 2 | {1, 2, 4} |
| RBF (IMQ) | eps*h | 1 | {0.5, 1, 2} |
| RBF (PHS r^3 + poly1) | none | -- | -- |
| tensor-product spectral | none | -- | -- |
| BWLer (explicit) | fd_k | spectral | {spectral,5,3} once, then freeze |

## 17.6 OPEN -- proposed: bake J/M = 1 as well
With lambda baked, Radon has two knobs (halo, J/M) and the tensor has one. Under 17.1 (tune on
everything) more knobs = more shots at the scored number. **Recommendation: bake J/M = 1 (balanced),
leaving both QI arms with exactly one knob and a symmetric tuning surface.** Supported by 14.1 at the
budgets that matter (balanced wins at C=2048); the one budget where balanced loses (C=512, J/M=1.3
wins) is already annotated as cliff-dominated and not a scaling point. Awaiting Sam.

## 17.7 Terminology (was never defined in this doc -- fixed)
**J** = number of DIRECTIONS (spokes), the angles theta_j = pi*j/J sampled around the half-circle.
**M** = number of OFFSETS per spoke, the parallel lines t_k along each direction.
Each neuron is one (theta_j, t_k) pair, so J*M ~ the neuron budget, and **J/M is how a fixed budget
splits between angular and along-ray resolution.** expH05 measured error falling exponentially in J
but only as a power law (~N^-10) in M, which is why the split is not neutral. The tensor has no
analogue -- it is forced symmetric at sqrt(N) per axis.


---

# 18. BUILD RECORD + COMPLIANCE AUDIT (2026-09-01, the build session)

## 18.1 Sam's rulings during the build (all applied)
- **x-axis label is W** (the plotted value remains the actual column count, per 16.6).
- **Completion order**: one line fills across all widths, then all methods on that
  task, then the next task; one full pass per regime, then the poly passes. (My
  first queue was width-outermost -- a violation of 13.13's stated order, caught
  by Sam, fixed.)
- **Colour groups by provenance**: bwler / darcy / dysts / other.
- **SOTA dotted line** per subplot: BWLer Table 2 for its five tasks; trained
  FNO ~1e-2 for darcy_orig (the expF08 convention). None published for
  poisson_man / darcy_man / dysts.
- **The dysts arm is IN** (both regimes): 8 systems, expF14 framing (lambda_max*T=3,
  6001-node mpmath references; MacArthur reference-capped at ~5e-14 by DOP853
  convergence). 13.15's "dysts are NOT part of expF17" was the builder's scoping
  note, now superseded.
- **poisson_cg stays dropped**; darcy_orig blocked pending a local copy of
  darcy_test_421_jax.npz (Zenodo's darcy_421.tgz is 12 GB train+test; the
  practical route is scp of Catherine's exported test-set npz from the cluster).
- **4096 extras and the PINN arm: queued strictly AFTER everything else.**

## 18.2 What is built
`experiments/expF17_method_scaling_suite/`: run.py + f17/{tasks,dicts,solve,
protocol,store,plots,tasks1d,dicts1d,solve1d}.py; tests in
tests/test_expF17_method_scaling_suite.py (25 tests). Queue: 3654 core cells
(2-D: 7 methods x 8 tasks x 5 widths x 3 seeds x 2 regimes + poly passes;
1-D: 6 methods x 8 systems x 6-8 widths x 3 seeds x 2 regimes). Resumable
JSONL store; replot every 15 landings.

Verification battery (all pass): oracle purity gate (FD residuals ~1e-50-ish at
fp64 tolerance; Cole-Hopf IC/BC; darcy control; 8 dysts references), exact
column identity at every rung, FD-checked derivative rows for all 13 dictionary
families (2-D and 1-D), seed-jitter reproducibility, step-0 anchors reproduced
(radon 4.6e-5 / tensor 4.2e-7 at n_ax=32/h4 on convection), spectral floor
(wave 3.2e-10 at n_ax=44), BWLer == Chebyshev span, min-norm lstsq semantics,
1-D QI floor (Lorenz 1.07e-13 at W=256), 1-D spectral dynamic floor (1.7e-14
at W=384), sampling-density rule.

Engine anchors vs recorded numbers: dynamic wave 4.8e-6 at C=1024 (expF13:
3.3e-6 at W=1024 with poly+collar); reaction cascade 1.7e-3 (expF13 1.5e-3);
GN calibration (17.2): all 7 families converge on reaction; the nu-continuation
ladder runs on all families for Burgers (shock-limited ~3e-1, expected); no
divergences, no adjustments needed.

## 18.3 Cole-Hopf correction found by the 16.8 re-verification
GH-75 is NOT sufficient on the full 200x200 grid: 1.0e-12 at t~1 near x=0 (the
t=0.5 slice hid it). Landed with n=200 nodes; GH-150+ agrees with GH-600 to
2.9e-15; independent trapezoid to 6.7e-16. `expF13/problems.py` now carries
exact_colehopf() as the burgers oracle (committed c521738).

## 18.4 NEW LOAD-BEARING RULE -- method-owned sampling density
Measured: the 1-D spectral arm under uniform-density data DIVERGES with width
(9.3e0 at W=256, degree 255) -- the Platte-Trefethen-Kuijlaars unstable regime
for high-degree polynomial regression on uniform samples. That line would have
measured sampling incompatibility, not the method. Rule adopted: **data and
collocation placement are method-owned and declared** -- spectral/BWLer draw
arcsine (Chebyshev) density on polynomial axes (uniform on Fourier axes),
snapping to the reference grid where truth is grid-bound; all other methods
draw uniform; counts identical for every method (4W). After the fix: spectral
oracle 4.1e-8 at W=256, dynamic 1.7e-14 at W=384 on Lorenz. The 2-D arm gets
the same rule (its degree <=44 masked the instability at the core ladder, but
4096/8192 extras would have hit it).

## 18.5 Builder decisions NOT explicitly ruled by Sam (flagged for review)
1. **Dynamic regime and poly variant inherit the oracle-tuned config** per
   (task, method, W). Defense: 17.1 dissolved the leak concern; equal effort in
   the dynamic arm = zero per-cell tuning for every method; 3x cost avoided.
2. **BWLer fd_k check closed by argument, not run**: under a direct lstsq solve
   there is no gradient optimizer for FD conditioning to help; the
   misspecification term is pure loss (15.4's own analysis). fd_k = spectral.
3. **rcond = 1e-15 in the 1-D arm** (13.7's global rule) where expF14's
   recorded default was 1e-13.
4. **J/M baked at the exact-identity split** (J = n_ax, M = n_ax+2, i.e. ~1) --
   17.6 adopted on the build go-ahead.
5. **1-D ladder = expF14's {48..384}** (per-system caps: MacArthur <=256,
   InteriorSquirmer to 768), not the 2-D {128..2048} -- d=10 systems make the
   block-GN quadratic in d*W, and expF14 located the interesting region here.
6. **dysts dynamic init = warm fp64 RK** (rtol 1e-6) -- expF14's default and
   dominant knob; an initializer, never an oracle.
7. **Reproduction gate implemented as the anchor battery** (18.2) rather than
   expF03's literal reproduction_gate(), because the geometry deliberately
   changed (per-spoke extents, no poly, endpoint rule).
8. **Tuning at seed 0 only**, then 3 seeds at the frozen config (14.2's rule).

## 18.6 Rulings received after the audit (2026-09-01, applied before launch)
- **Queue = plot-resolution order**: 1a resolves fully (oracle, scaling methods,
  all 16 tasks), then 1b (dynamic, scaling), then plot 2 (comparators, oracle
  then dynamic), then plot 3 (poly). Within a phase: task -> method -> W -> seed.
- **Dynamic arm owns one declared solver knob**: w_mult in {0.3, 1, 3} on the
  BC/IC block weights, swept at seed 0 in the dynamic regime (dictionary knobs
  inherited from the oracle tuning), frozen, then seeds -- identical declared
  effort for every method. PINN tuning to be declared when that arm is built.
- **Plot 4 = residual fields** at each task's best landed W (oracle, seed 0
  refit): 4a QI-Radon and 4b QI-tensor heatmaps (log10 error, [-1.25,1.25]^2
  canvas, train/score box drawn, outside-the-box and holes masked out of the
  log scale), 4c QI(grid) per-component |error(t)| lines for dysts; regenerated
  at every task-completion boundary. The former dysts scaling figures are
  renumbered to plot1a/1b dysts pages.
- **The 16-task list confirmed**: 8 PDE (2-D) + 8 dysts (1-D). Colour groups
  bwler/darcy/dysts/other. fd_k rationale explained and accepted as frozen.
- **Translation triple-check**: new end-to-end test drives the exact solution
  through OUR assembled operator rows for the coefficient-folding tasks
  (convection, wave, darcy_man); poisson_man excluded there (log singularities
  break the spectral-residual premise; its map is scale-invariant and
  FD-verified in expF13).
- Store wiped to `*.pre-protocol-freeze.jsonl.bak` before launch so every
  landed cell is under the final frozen protocol.

## 18.7 Rulings during the run (2026-09-01, ongoing)
- **Edge-walk tuning rule ADOPTED** (amends 17.5): argmin on a sweep boundary
  extends the grid one step outward at its own spacing, at most twice --
  identical declared effort for every knobbed method. Motivating measurement:
  ELM chose the R=4 boundary in 60/84 sweeps (Dong & Yang's width-growing
  optimum); 139 affected (task, method, W) combos invalidated and re-run.
- **1-D ladder = {64, 96, 128, 192, 256, 384, 512}**, uniform across systems
  (48 dropped as below-resolution; MacArthur cap and InteriorSquirmer 768
  exception dropped; off-ladder landed cells purged).
- **Dynamic arm ON HOLD** at Sam's instruction pending tuning-fairness review
  (an independent fresh-context reviewer is auditing the knob table, the PINN
  tuning question, and the dynamic-inheritance assumption).
- **Plot conventions**: discrete labeled ticks at the protocol widths (log
  scale kept); dysts panels x-window (56, 590); chosen halo printed in
  residual-panel titles; plot-4 legend is a single two-row figure legend.
- **NEXT PHASE (Sam): reproduce published baselines in-house** rather than
  taking literature numbers on faith -- the BWLer Table-2 values, Dong & Yang
  ELM optima, and the trained-FNO ~1e-2 Darcy figure are currently cited as
  SOTA reference lines but have not been reproduced under our harness. Until
  then they are annotations, not measurements.

## 18.8 FINAL PROTOCOL AMENDMENTS from the tuning-fairness review (2026-09-01, Sam-ruled)
The independent reviewer verified: dynamic inheritance of oracle-tuned knobs (4
spot re-tunes reproduce the inherited argmin), the w_mult grid, the RBF-IMQ
walk bound (rank collapses below epsh 0.125), PHS tail degree (not first-order
for global fits), the ELM bias convention, and the fd_k argument. It found two
material defects, both flattering the QI arms:
1. **The balanced-square rule was the spectral result on anisotropic tasks**:
   shaped spectral (6 Fourier x 88 Chebyshev) reaches 5.5e-15 on convection_c80
   at ~529 columns where the square shape records ~1.0 across the ladder.
   RULING: grid comparators (spectral, bwler, rbf_imq, rbf_phs) get a declared
   aspect knob, ny/nx in {1/4, 1, 4} at fixed budget with the standard walk
   (rbf_imq sweeps epsh x aspect factorial, 9 cells); the QI arms stay balanced
   -- J/M keeps its direct 14.1 measurement and stays BAKED. Reproduced in-build
   before freezing.
2. **The 1-D ELM walk cap bound its line 41-326x above optimum at large W**
   (optima at R 16-32, cap was 16). RULING: walk cap raised 2 -> 4 extensions
   per knob for every knobbed method.
Further rulings: halo gets a +-1 REFINEMENT around the final argmin (halo is
coarse and the QI arms are sensitive near 8; refinement may probe halo 0);
w_mult gets the same walk (measured optimal at its boundary, so this is
consistency, not correction); tuning records carry the full evaluated grid.

**PINN protocol (adopted, cost-trimmed at Sam's instruction).** Single-hidden-
layer tanh at the cell's actual column count, fp64, default init; Adam peak lr
cosine-decayed over 20k steps then L-BFGS polish (cap 2k); lr swept {3e-4,
1e-3, 3e-3} at seed 0 with the standard walk; dynamic adds the w_mult sweep;
iteration budget FIXED (not a knob), wall-clock disclosed. Load-bearing
certificate: every trained PINN also lands a frozen-feature lstsq REFIT of its
readout (rcond 1e-15) -- the exact optimum over everything optimizer tuning
could reach given the learned geometry -- plotted as a second linestyle.
BWLer's published vanilla-PINN numbers plotted as reference dots on its tasks.
2-D tasks only. PINN is ON the plot-1 scaling axis (same architecture, trained).

**CLEAN SLATE (Sam).** All stores and generated plots archived to
archive/pre-clean-run/ and the suite re-runs once, uninterrupted, under this
frozen protocol. No mid-run protocol edits: any future change stops the run,
wipes, and restarts. Plot 3 shows exactly 4 lines (radon/tensor x with/without
poly); the best-spectral reference line is dropped from it.
